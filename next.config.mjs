/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "f.hubspotusercontent00.net",
      "comprerural.com",
      "cerradocase.com.br",
    ],
  },
};

export default nextConfig;
